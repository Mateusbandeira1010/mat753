<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="/css/style.css">
    <title>{{title}}</title>
<body>
    <header class="navbar header">
        <div class="container">
            <a class="navbar-brand" href="/">
                <h1>M4STOREEE</h1>
            </a>
           <div class="d-flex">
                {{#if isAuthenticated}}
                    <!-- Se o usuário estiver autenticado, não mostra o botão de Login/Cadastro -->
                {{else}}
                    <a href="/loginCad" class="btn btn-outline-light me-2">Login/Cadastro</a>
                {{/if}}
                <a href="/" class="btn btn-outline-light me-2">Inicio</a>
                <a href="/Meu-usuario" class="btn btn-outline-light me-2">Usuario</a>
                <a href="/fluxoCaixa" class="btn btn-outline-light me-2">Fluxo-caixa</a>
            </div>
        </div>
    </header>



 <main class="container mt-4">
        {{{body}}}
    </main>





</body>
</html>


